---
name: refund-tax-skill
description: 通过退税 MCP 服务识别护照或其他证件，并按证件签发国家或地区代码和证件号码查询退税税单列表及详情。适用于证照识别、退税记录查询、退税申请单详情查询；不用于修改、审批或删除退税数据。
---

# 退税税单助手

使用本技能声明的 `refund-mcp-server` MCP 工具完成证照识别和税单查询。工具参数和返回约定见 [references/mcp-tools.md](references/mcp-tools.md)。

## 路由请求

- 用户提供证件图片并要求识别时，调用 `recognize_document`。
- 用户提供证件签发国家或地区代码及证件号码时，调用 `list_tax_bills`。
- 用户提供退税申请单号时，调用 `get_tax_bill_detail`。
- 用户要求从证件开始完成查询时，依次识别证件、查询列表，并按用户选择查询详情。

只调用完成当前请求所需的工具。除非用户明确要求，不批量查询列表中每张税单的详情。

## 识别证件

1. 仅处理用户在当前任务中明确提供或指定的文件，不扫描其他文件或目录。
2. `recognize_document` 需要真实文件内容的 Base64，不能传本地路径。
3. 对有本地路径的文件，优先运行 `scripts/encode_document.py <path>`，读取其 JSON 输出并将 `fileName`、`contentType`、`fileBase64` 原样传给工具。不要在回复中展示 Base64。
4. 若附件没有可读取的本地路径，使用当前环境提供的文件读取能力获取字节；无法获取时，请用户重新上传或提供可访问路径。
5. OCR 缺少 `countryCode` 或 `documentNumber`、出现多个候选值，或结果明显不完整时，先请用户核对或补充，不要猜测。

## 查询税单

调用 `list_tax_bills` 时：

- `countryCode` 使用证件签发国家或地区代码；仅在含义明确时转为大写。
- `documentNumber` 使用证件号码，不自行增删字符。

以简洁列表展示税单的可识别信息和退税申请单号。默认遮盖证件号码，只保留末四位；用户明确要求时才完整展示。

## 查询详情

调用 `get_tax_bill_detail` 时，将用户提供或列表返回的 `refundApplicationNo` 原样传入。

- 列表只有一条且用户已明确要求查询详情时，可以直接继续查询。
- 列表有多条时，先展示列表并请用户选择，除非用户明确要求查询全部详情。
- 仅展示工具实际返回的数据，不推测缺失字段。

## 失败处理

- MCP 不可用时，提示确认 `http://127.0.0.1:8081/mcp` 服务正在本机运行，然后停止重试。
- 工具返回失败时，保留并说明其 `code` 和 `message`；不要把失败包装成成功结果。
- 参数不足时，只询问完成当前调用所缺少的字段。
- 本技能只读，不执行税单创建、修改、审批、退款或删除操作。

